# Int_ArrayList

Simplified custom implementation of an integer-only dynamic array, similar in functionality to Java’s built-in ArrayList. It allows adding, retrieving, updating, and removing elements, while automatically resizing its internal storage array as needed.

## Backend:
- `.java` Java v.17

## Tools:
- Git and GitHub for version control
- CodingRooms for web-based editing
- Visual Studio Code for local editing
